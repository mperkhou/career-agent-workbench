# 2.1.0 career-agent-workbench-2-1-0-documentation-parity

Release: `2.1.0`

Objective: Bring the public documentation to relative parity with the mature
archived project documentation by replacing the intentionally tiny fictional
resume text and Master Resume Object (MRO) with a realistic, full-depth,
public-safe demo; rebuilding every README example from that approved demo; and
reassessing each diagram and Flask screenshot without exposing private career
state. Product work remains limited to the separately approved compatibility,
typed remote-API retry-contract, additive sanitized-diagnostic compatibility,
first-draft ATS-diagnostics serialization, and per-attempt deadline/embedded-
provider-envelope repairs needed to execute and explain the synthetic
documentation workflow safely.

## Operating Rules

- Follow root `AGENTS.md` before editing, validating, committing, pushing, or
  closing out release work.
- Treat this committed plan as the canonical workflow plan.
- Use runtime state in
  `tmp/agentic-workflows/career-agent-workbench-2-1-0-documentation-parity/tracker.json`
  as the local cursor and evidence log, not as a second implementation plan.
- Treat P steps as implementation phases and G steps as reassessment gates.
- At each G step, compare completed work against the remaining plan and either
  continue, amend incomplete/future work, or pause for user input.
- Gate amendments may change only incomplete/future work. Update this plan and
  the same-version changelog together, commit normally, and rebind the ignored
  tracker without rewriting completed history.
- Do not begin P01 until the user explicitly approves this initialized plan.
- Do not push, create or mutate a PR, merge, tag, publish, call a provider or
  model, fetch a new job posting, contact a third party, or mutate retained
  private profile, application, tracker, or generated-artifact state without
  separate explicit authority.
- Handle private source material read-only and outside the public repository.
  Never place original excerpts, private-source-bearing transformation drafts,
  mapping tables, screenshots, logs, or content-bearing evidence under the
  repository root, including ignored paths.
- Store only sanitized counts, ratios, hashes, booleans, route summaries, and
  findings in the ignored tracker. Keep sensitive or heavy evidence under
  `$TMPDIR/career-agent-workbench-agentic/career-agent-workbench-2-1-0-documentation-parity/`
  and delete it promptly after its matching gate.
- Use one main-agent owner for all private-source reading and candidate edits.
  Evidence routes may inspect only already-public candidates or content-free
  metrics; they must never receive the private source material.

## Baseline Binding

- Public repository base commit:
  `91defe87f12515afe18384307f411ec4e07c2be7`.
- The public base is simultaneously `main`, `origin/main`, and tag `v2.0.0` at
  initialization. The v2.0.0 publication process is outside this workflow.
- Archived documentation source: tracked `README.md` in the archived
  `linkedin-career-mcp` checkout at commit
  `534d503f40ae06fe1ecf0a93d4d8b9c916d7d8f5`.
- Archived README Git blob:
  `8a8e884cb06522688593902e3409f2dcbb01d201`.
- Archived README SHA-256:
  `c0cf273e684751ba0a2ff187a0a885188274c8e99620e7698614da35132e9cfd`.
- The bound README is tracked-clean and contains 719 lines, 5,137 words, and
  39,999 bytes. These measurements are comparison aids, not quotas.
- Dirty private profile files and unrelated implementation files in the
  archived checkout are explicitly excluded from the bootstrap. No private
  resume or MRO content was read, copied, hashed, summarized, or measured while
  initializing this plan.

## Documentation Scope

In scope:

- `examples/demo-workspace/profile/MP-MASTER-RESUME.txt` and
  `examples/demo-workspace/profile/MASTER-RESUME.yml`;
- the demo description, deterministic job fixture, materializer, and focused
  tests where needed to keep the larger public demo coherent and offline;
- the bounded Make, CLI, configuration-precedence, retry, timeout, and Flask
  action-diagnostic compatibility repairs defined by P03C, plus P03D's typed
  remote-API retry and additive failure-subtype compatibility repair, and
  P03S's demonstrated first-draft ATS-diagnostics serialization repair, plus
  P03U's approved v1/v2 per-attempt hard-deadline and HTTP-200 embedded-
  provider-envelope correction, plus P03V/G03V's separately gated synthetic
  documentation-evidence sequence, revision 15's consumed implementor-owned
  corrective preflight, and revision 16's one-time supervisor-owned direct
  model-free preflight, plus revision 17's user-approved single live subphase
  after that preflight passed;
- `README.md`, its examples, and its linked documentation assets;
- the two workflow diagrams and the existing Flask screenshot set, subject to
  their explicit reassessment gate;
- v2.1.0 version, changelog, release-note, packaging, and validation alignment.

Out of scope:

- Flask, CLI, MCP, matching, resume-generation, or application-state feature
  changes except the narrowly demonstrated compatibility repairs needed for
  the larger deterministic demo, P03C execution-configuration parity, and
  P03D typed remote-API retry/additive-subtype work, plus P03S's single
  first-draft producer/state-contract repair and P03U's bounded v1/v2 model-
  attempt deadline and provider-envelope repair;
- a generalized durable-diagnostics platform for direct CLI, Make, and Flask
  execution, including cross-process owners or leases, restart/crash recovery,
  multi-job or multi-stage run correlation, truncation aggregation, durable
  final-summary state, a structured diagnostic viewer, and composite-Make run
  ownership. Preserve the existing P03C Flask action log; defer this broader
  product architecture to a future feature release;
- new documentation features beyond parity with the bound archived baseline;
- live provider/model calls beyond the consumed revision-10 P03R traversal,
  the consumed post-P03S P03T sequence, and at most one P03V sequence after
  its model-free preflight passes and the user separately approves its live
  subphase. Binding the original revision-14 P03V amendment authorized neither
  its preflight nor a provider/model/Codex call; revision 15's corrective
  preflight authority is consumed; and revision 16 authorizes only one direct
  supervisor-owned model-free preflight. Revision 17 alone authorizes the one
  conditional P03V live sequence after the user reviewed and approved that
  passing preflight;
- authenticated browsing, application submission, employer contact, or changes
  to retained private workspace state;
- copying any archived private example, job row, generated artifact, dotenv,
  database, log, cache, profile file, or machine-specific path;
- push, PR, merge, tag, package publication, or release publication.

## Anonymization Contract

- The public candidate must be a coherent fictional person and must be labeled
  as fictional wherever a reader could mistake it for a real profile. Use only
  reserved example domains and public-safe fictional contact data.
- Replace names, employers, titles, locations, schools, issuers, teams,
  customers, internal systems, product-specific identifiers, project names,
  URLs, dates where chronology could identify the source, and any unique
  combination that could link the candidate back to a real person or employer.
- Preserve the professional-skills taxonomy and technical substance as closely
  as safety permits. Generic skill names may remain; private aliases, internal
  tool names, and uniquely identifying groupings may not.
- For the single longest source role designated privately by the operator,
  preserve the long-form paragraph style and representative technical themes
  while reducing its paragraph/bullet count and source-word volume to roughly
  45-55 percent. Replace its employer, title, location, chronology, products,
  organizational references, and linkable phrasing.
- For every other role, retain the complete item count, approximate per-role
  depth, and substantive coverage. Do not trim content merely to simplify the
  demo; rewrite or generalize only as needed for fictional coherence and
  de-identification.
- Preserve the count, structure, and realistic feel of education, portfolio,
  and certification sections while replacing every identifying institution,
  issuer, project identity, repository identity, credential identifier, and
  link with coherent fictional equivalents.
- Keep dates, promotions, role ordering, locations, education, credentials,
  and portfolio history internally consistent after fictionalization.
- Do not send private text, distinctive phrases, or private identifiers to an
  internet search engine, external model, or evidence route. All source-to-demo
  comparison is local and content-hidden outside the main-agent workstream.
- Measure parity section by section. The designated long role must satisfy its
  45-55 percent target; other roles must preserve their source item counts and
  remain within a reasonable per-section word-volume band unless privacy
  requires a documented exception. Git and tracker evidence records only the
  ratios and pass/fail result, never the source values or mapping table.

## Steps

### P01 - Full Public Demo Resume Text

- Reconfirm branch, clean worktree, plan digest, target version, private-source
  containment, and the user's explicit approval before reading source content.
- Create the external evidence area with restrictive permissions. Inspect the
  operator-selected private resume text there, one section at a time, and keep
  its source inventory and transformation map outside Git.
- Replace the tiny tracked fictional text with a complete candidate that
  follows the anonymization contract, including the special 45-55 percent
  treatment for the designated longest role and full-depth treatment for every
  other role.
- Run local-only identifier, reserved-domain, chronology, section-completeness,
  length-ratio, public-tree safety, and focused demo checks. Review the staged
  candidate for distinctive phrases and unique fact combinations that a
  pattern-based scanner would miss.
- Commit only the public-safe candidate and any focused test adjustment needed
  to express its intended structure. Do not derive the YAML MRO yet.

### G01 - Resume Text and Privacy Review

- Present the public candidate, changed-path list, section inventory, and
  content-free parity ratios for user review without reproducing private source
  excerpts or mappings.
- Require explicit user acceptance of the fictional identity, technical
  substance, designated-role reduction, other-role completeness, and
  education/portfolio/certification treatment before P02.
- Continue only if both the automated safety check and deliberate human
  de-identification review pass. Otherwise amend only future work or revise P01.

### P02 - Derived MRO and Coherent Offline Demo

- Use the repository's `master-resume-yaml` skill to derive the public
  `MASTER-RESUME.yml` from the G01-approved public text, not from a separately
  anonymized private YAML file.
- Inspect the operator-selected private MRO read-only in the external evidence
  area only as a schema, section-depth, linkage-density, and size reference.
  Do not copy its private values or treat it as an independent factual source.
- Preserve the complete public evidence structure: neutral render flags, skill
  categories and match terms, professional-experience ownership and ordering,
  source evidence, and realistic education, portfolio, and certification data.
- Keep the public YAML comparable to the private reference in section coverage
  and structural depth after accounting for the approved longest-role
  reduction. Record only content-free ratios and pass/fail findings.
- Prove every MRO claim traces to the approved public text and that no
  unsupported skill, metric, employer, credential, or outcome enters the YAML.
- Update the fictional demo description, deterministic job fixture,
  materializer expectations, and focused tests only where the expanded MRO
  makes that necessary. Keep the demo offline, idempotent, and confined to a
  fresh disposable workspace.
- Render and inspect the demo resume HTML and stored objects from a fresh demo
  materialization. Store screenshots and rendered artifacts outside Git for
  the gate, then delete them after review.

### G02 - MRO Lineage and Demo Review

- Compare the approved text, MRO, rendered resume, demo ARO, and focused test
  evidence for structural completeness, claim lineage, formatting, and
  fictional coherence.
- Require public-tree safety, reserved contact endpoints, zero private names or
  identifiers, exact source-evidence ownership, and a clean from-scratch
  `make demo` path.
- Present the public text and YAML together for explicit user review. Do not
  begin the README rewrite until the user accepts the full demo pair.

### P02R - Full-Size Fictional Job Fixture Refinement

- Inspect a bounded sample of retained private job descriptions read-only to
  derive only content-free length, section, and formatting patterns. Keep raw
  descriptions, excerpts, employer facts, identifiers, and comparison notes
  outside the public repository, and do not mutate private tracker state.
- Replace the compact deterministic target-job description with an original,
  full-size fictional posting for the existing fictional employer and role.
  Use reserved endpoints and realistic overview, responsibility,
  qualification, preferred-skill, work-model, and benefits sections without
  copying source wording or representing a real opening.
- Keep the fixture coherent with the approved public resume and useful for
  later README demonstrations, while retaining believable gaps so the match is
  not artificially perfect. Update the demo materializer and focused tests
  only where needed for the expanded description.
- Re-run the disposable idempotent demo, stored-object and Flask presentation
  checks, focused tests, Ruff check/format, `git diff --check`, and public-tree
  safety. Commit locally and stop before README work.

### G02R - Expanded Job Fixture Review

- Present the fictional posting, public-only size and section inventory, and
  content-free comparison bands for explicit user review.
- Require realistic depth, fully original wording, reserved endpoints, no
  private employer or posting identifiers, a clean from-scratch demo, and
  explicit user approval before P03.

### P03 - README Narrative and Example Parity

- Build a section-by-section parity matrix from the bound archived README and
  the current public application. Use the archived document's explanatory
  density and ordering as the baseline while treating current public behavior,
  commands, safety boundaries, and product naming as authoritative.
- Rebuild the public README to cover the mature baseline's product overview,
  terms, master-resume setup, application workflow, JOD rewrite example,
  second-pass workflow, Flask tracker, commands, MCP tools, configuration,
  operational limits, development, and release closeout.
- Regenerate every resume, MRO, ARO, JOD, ATS, variant, and workflow example
  from the approved fictional demo. Do not copy real companies, institutions,
  metrics, job IDs, cached paths, private model output, or application examples
  from the archived document.
- Verify every described interface and command against the v2.1.0 tree. Omit or
  clearly qualify archived behavior that the public workbench does not support.
- Aim for comparable depth to the 5,137-word baseline, with section coverage
  and factual accuracy taking precedence over a forced word count.

### G03 - README Parity and Screenshot Strategy

- Review the parity matrix, narrative diff, example lineage, link targets, and
  command verification. Require explicit user approval of the README direction
  before visual-asset work.
- Choose one bounded screenshot strategy:
  1. retain a current tracked annotated screenshot only after confirming it is
     still accurate and contains no private or application-derived state; or
  2. regenerate it from an approved synthetic workspace in a fresh disposable
     environment. When a separately authorized live synthetic traversal is
     used, capture only its reviewed stored state after all network and model
     activity has stopped.
- A new real public job posting is optional and requires separate user approval
  of the exact posting plus confirmation that it is not an existing
  application. The workflow does not query private tracker state or fetch a
  posting merely to make the screenshots look more realistic.
- Amend P04 if the approved asset strategy materially changes its responsibility
  surface.

### P03C - Workflow Configuration and Diagnostic Parity

- Treat the content-free v4.12.0 mature-checkout handoff as bounded migration
  evidence only. Do not copy or inspect credentials, private dotenv values,
  private career content, generated artifacts, or source logs. Keep the live
  synthetic traversal paused and make no provider/model call during P03C.
- Align public effective defaults and direct-script behavior with the mature
  workflows: v1 uses 300 seconds per individual LLM call, v2 uses 600 seconds
  for its critique call, and manual/highlighting use 900 seconds per Codex
  subprocess. All four default to one retry after the initial attempt. Preserve
  the user-approved live-run override of two retries as three total attempts.
- Keep the existing manual profiles (`regular` Sol/high, `economy` Terra/high,
  `premium` Sol/xhigh) and change the highlighting default to Luna/high. Make
  model and reasoning-effort precedence equivalent in practice across direct
  CLI, Make, process environment, private dotenv, shared compatibility values,
  and defaults. An explicitly present empty reasoning effort must mean inherit
  Codex CLI configuration rather than being replaced by a workflow default.
- Make workflow retry loops timeout-only. v1 and v2 may retry only a typed
  timeout; manual and highlighting may retry only Codex subprocess timeout.
  Schema/JSON, response, evidence/policy, rendering, state, artifact, missing-
  output, and nonzero-exit failures must fail that row/stage without consuming
  a workflow retry. Preserve row isolation so later jobs still run and earlier
  successful stages remain stored.
- At workflow startup, emit a sanitized effective-configuration event with the
  stage, final model, final effort (including explicit inheritance), per-call
  or per-process timeout, retry count, total attempts, source layer
  (`cli`/`make`/`process`/`private_dotenv`/`default`), and only a boolean for
  workspace configuration. Emit content-free attempt, elapsed-time, timeout,
  retry-decision, completion, and closed failure-category events. Never emit
  keys, paths, job/resume content, prompts, responses, or raw exception text.
- Preserve the compact Flask status UI while making the bounded complete
  per-run action history available. Store only sanitized status events in a
  mode-0700 external workspace tmp directory with mode-0600 files, bounded run
  count/size, and safe replacement/cleanup behavior; also expose the complete
  retained message set through an explicit full-detail or paginated local
  status response instead of the current 20-message projection. Do not persist
  raw subprocess/model traffic or command paths.
- Add a model-free effective-configuration preflight for each workflow so the
  exact Make/CLI/private-dotenv result can be verified after the operator fixes
  the private environment. Prove the unprefixed Make variables control Flask-
  launched nested Make runs and document that prefixed compatibility values do
  not override explicit Make flags.
- Add focused configuration, Make composition, retry matrix, batch-isolation,
  sanitized logging, Flask status, external-log containment, and restart/
  bounded-retention tests. Run Ruff, diff checks, public-tree safety, and the
  proportional focused suite. Commit P03C independently before any live call.

### G03C - Execution Parity and Preflight Review

- Present the exact public/mature parity matrix, resolved-default matrix,
  timeout-only retry proof, Make/CLI/env precedence proof, empty-effort proof,
  model-free preflight output, status-log inventory, changed paths, and
  validation results.
- Require a corrected operator-owned private dotenv, a sanitized preflight that
  proves the expected workspace/model/effort/timeout/retry sources without
  exposing values beyond approved model labels, and explicit user authority
  before authorizing a fresh provider run.
- Keep the existing P03R workspace and all four snapshot directories intact.
  If parity requires broader runtime redesign or the preflight remains
  ambiguous, pause rather than calling a provider.

### P03D - Typed API Retry and Additive Diagnostic Repair

- Treat P03C/G03C as completed historical evidence of the then-implemented
  timeout-only workflow contract. Do not rewrite those completed results or
  any retained P03R attempt. The corrected P03R run established one narrower
  defect: API-backed v1/v2 workflows own a visible retry budget but retry only
  `ModelTimeoutError`, while their one-attempt workflow API client collapses
  other provider failures into generic `LlmError`. Repair that demonstrated API
  path prospectively; do not claim provider-agnostic transient-retry parity.
- Keep the v1/v2 workflow layer as the single retry owner for every API-backed
  logical operation. A configured `retry_count` permits at most
  `retry_count + 1` provider boundaries including the initial request. Keep
  workflow-created API clients at one internal attempt, while preserving any
  compatible direct-client behavior outside that construction path. Do not
  broaden manual-pass or highlighting Codex retries. Keep non-timeout Ollama
  failures non-retryable in 2.1.0; its existing typed-timeout behavior remains
  unchanged and must be documented honestly.
- Define typed retryable and non-retryable API/model failures rooted
  compatibly in `LlmError`, with `ModelTimeoutError` remaining a distinct typed
  timeout marker. Classification must use type and validated closed metadata,
  never exception-message parsing. Exception messages and structured metadata
  must be content-free, and diagnostics must never serialize tracebacks, raw
  exception messages, prompts, outputs, response bodies, credentials, URLs,
  paths, private headers, or provider-private text. A validated numeric HTTP
  status and bounded delay value may be retained only for the closed contract.
- Retry only HTTP 408, 409, 425, 429, 500, 502, 503, and 504; sanitized remote
  connect, read, and protocol interruptions that are reasonably transient;
  valid supported API envelopes containing an empty generation; and the
  existing typed timeout. Preserve bounded exponential backoff and bounded
  `Retry-After` handling without retaining raw headers or creating nested
  attempts. Cap accepted `Retry-After` timing at 120 seconds and fallback
  exponential backoff at 60 seconds.
- Keep permanent HTTP failures, malformed or unsupported envelopes, response-
  size violations, syntactically invalid generation JSON, valid-JSON schema or
  application rejection, unexpected model failures, and evidence/policy/state/
  render/ATS/local-path/artifact failures non-retryable. For v2, classify
  syntactically invalid generated JSON before reporting the logical operation
  complete; leave schema and application validation after valid JSON outside
  the retry loop.
- Preserve every broad diagnostic category, including `category=model`, and
  add only an optional validated `failure_subtype`. The closed subtype
  vocabulary remains `timeout`, `transient_http`, `permanent_http`,
  `transport_read`, `transport_protocol`, `transport_connect`,
  `empty_completion`, `malformed_envelope`, `invalid_generation_json`,
  `response_too_large`, and `unexpected_model`. Existing P03C/P03R events and
  status records without a subtype must remain readable; do not rewrite or
  delete retained history.
- Use the existing P03C stderr event stream and Flask-owned bounded action log.
  Emit content-free stage, attempt N/M, elapsed time, broad category, optional
  subtype, and retry decision events so the next separately authorized single-
  job traversal can distinguish a transient retry, exhaustion, and an immediate
  non-retryable failure. Direct CLI remains stderr-only for these events and
  Make remains a command composer. Do not add a direct/Make durable recorder,
  run IDs, leases, cross-process ownership, restart recovery, multi-job
  correlation, truncation aggregation, durable final summaries, a structured
  viewer, or composite-Make ownership semantics in 2.1.0. Those form one
  deferred feature architecture; leave the committed P03C Flask persistence
  behavior intact.
- Add focused mocked/fake tests for 429 then success; 502, 503, and 504 then
  success; transient connect/read/remote-protocol interruption then success;
  empty completion then success; timeout then success; exhausted transient
  attempts; permanent 400, malformed envelope, response-too-large, invalid-
  generation JSON, and unexpected failures without retry; v2 parity; and exact
  provider-boundary counts that never exceed `retry_count + 1`. Prove the
  workflow API client has one internal attempt, legacy diagnostics without a
  subtype still load, subtype classification is independent of raw exception
  text, and diagnostic output excludes prompts, responses, keys, URLs, paths,
  raw exception text, job IDs, and private content. Preserve focused tests for
  unchanged Ollama timeout and manual/highlighting behavior.
- Keep direct CLI/Make path handling compatible and include every retained new
  module/test in the explicit source distribution membership. Align the
  retained `2.1.0` version, `CHANGELOG.md`,
  `docs/release-notes/2.1.0.md`, and README operational text with the verified
  API-specific contract without claiming a successful live traversal.
- Keep P03D completely offline: do not resolve the private dotenv, invoke a
  provider/model or Codex subprocess, mutate the retained synthetic workspace,
  or consume renewed P03R authority. Preserve the deterministic demo, P03R
  workspace, four empty mode-0700 snapshot directories, completed commits, and
  tracker history.
- Run the focused and proportional regression suites, Ruff check/format,
  `git diff --check`, public-tree safety, and a deliberate privacy/scope review.
  Commit P03D normally and stop at G03D with a clean worktree. Do not bundle or
  authorize another force-v1 traversal.

### G03D - API Retry Contract and Diagnostic Compatibility Review

- Present the typed retryable/non-retryable API contract, transient status and
  transport matrix, v1/v2 exact `retry_count + 1` provider-boundary proof,
  bounded backoff evidence, v2 invalid-JSON behavior, and the explicit API-only
  2.1.0 scope. Confirm unchanged non-timeout Ollama, manual-pass, and
  highlighting retry behavior.
- Present additive subtype and legacy-load evidence through the existing P03C
  stderr/Flask capture, direct/Make path compatibility, source-distribution
  membership, changed paths, documentation/version alignment, commits, and
  complete validation results. Confirm that the deferred durable recorder,
  lease, cross-process ownership, restart, aggregation, correlation, and viewer
  architecture did not enter this release.
- Require one visible API workflow retry owner, no nested attempts, exact
  boundary counts, readable retained P03C/P03R history, content-free diagnostics,
  and a clean public-safety review before accepting P03D.
- Pause for explicit user review. A renewed live force-v1 traversal remains a
  separate plan amendment and approval boundary after G03D; no provider/model
  call is authorized by completing or approving this repair.

### P03R - Recorded Synthetic Live Traversal

- P03R was paused after its consumed post-G03C authority. P03D completed and
  G03D passed under revision 9; on 2026-08-04 the user approved G03D and
  explicitly selected renewal of the recorded synthetic traversal. Revision
  10 therefore authorizes exactly one new post-G03D P03R sequence and no
  operator rerun. Reuse the preserved external mode-0700 workspace built from
  the tracked Tessa Rowan and Rivermark demo. Resolve
  configured provider credentials without printing or copying them, and
  override every workspace member explicitly so no retained private profile,
  database, artifact, or tracker state can be read or changed.
- Before any provider or Codex subprocess call, run one sanitized model-free
  preflight against the exact execution path. It must prove the configured
  synthetic workspace, canonical non-aliased workspace/template paths, approved
  model/effort labels, 300/600/900/900-second timeouts, retry counts of two,
  three total attempts, and their configuration sources without printing
  credentials, private values, raw paths, prompts, outputs, or career content.
  A failed or ambiguous preflight stops at G03R without making a live call and
  ends this revision-10 execution opportunity. A corrected preflight or live
  resume would require another committed amendment and explicit user approval.
- Before the first P03R live request, the demonstrated public batch-query
  incompatibility was repaired and committed: v1 generation, ARO regeneration,
  v2 refinement, and highlighting now import and pass the canonical
  `MAX_QUERY_RESULTS` instead of the invalid literal `10_000`, without changing
  the state API bound or selection, filtering, or generation behavior.
- Under the revision-10 renewal, do not search for or fetch a job. Run only
  `demo-platform-001` through a bounded live sequence: force-regenerate `v1`,
  derive evidence-grounded `v2`, derive `manual` from `v2` with the `regular`
  manual profile, then highlight the automatically selected `manual` variant
  under the independent highlighting policy.
- Run the traversal with `FIRST_DRAFT_LLM_RETRIES=2`,
  `SECOND_PASS_RETRIES=2`, and `CODEX_RETRIES=2`. In the current adapters each
  value permits two retries after the initial request, for at most three
  provider boundaries per API-backed logical operation or three Codex
  subprocess attempts. The P03D API contract may now spend that same visible
  v1/v2 budget on a typed timeout or narrowly allowlisted typed transient
  provider failure; non-timeout Ollama and manual/highlighting behavior remain
  unchanged. Never add an operator retry around an evidence,
  unsupported-claim, stale-revision, or policy rejection merely to obtain a
  cleaner demonstration.
- Before the later post-revision-6 provider call, a narrow content-free first-
  draft diagnostic boundary was added, tested, validated, and committed so a
  per-record failure reports its bounded local stage and sanitized exception
  category without recording prompts, responses, resume or job content,
  credentials, paths, or raw exception text.
- The revision-6 recovery command was intentionally interrupted after three
  HTTP-boundary invocations when the operator identified a misconfigured
  private dotenv; it stored no live variant and authorized no further call.
  After P03C/G03C and private configuration correction, the user separately
  authorized one fresh bounded force-`v1` run with
  `FIRST_DRAFT_LLM_RETRIES=2`. That consumed run retained the existing
  disposable workspace and four snapshot directories. Its contract required a
  stop with sanitized evidence after any failed stage and forbade another
  operator rerun; therefore v2, regular manual, and highlighting did not begin.
- The single corrected post-G03C force-`v1` run is retained as completed P03R
  history: core and JOD succeeded, the first experience rewrite failed after
  three total provider boundaries with broad category `model`, zero workflow
  retries consumed, no stored live variant, and no v2/manual/highlighting
  mutation. That consumed its authority. P03D/G03D must complete before any
  future live run is considered, and any renewed force-`v1` traversal requires
  a separate explicit plan amendment and user approval.
- The revision-10 authorization applies only to one force-`v1` invocation for
  `demo-platform-001` followed, on verified success, by one `v2`, one `regular`
  manual pass, and one highlighting invocation. Stop immediately at the first
  failed stage, preserve its sanitized full Flask action log and every prior
  successful stage, and return to G03R. Do not use this authority for a second
  force-`v1`, a job fetch/search, a different row, a private workspace, or a
  compensating wrapper retry.
- After each stage, verify MRO/JOD evidence ownership, variant lineage,
  selection behavior, accepted/rejected/unsupported changes, ATS diagnostics,
  stored YAML/HTML/PDF artifacts, model metadata, and human-review state.
  Render every PDF page and inspect legibility, overlap, wrapping, and factual
  completeness.
- Keep the disposable database, raw logs, rendered artifacts, model traffic,
  credentials, and complete evidence packet outside Git. Commit only vetted
  public-safe README excerpts and aggregate findings that are traceable to the
  approved synthetic inputs and reviewed stored outputs.
- Before each later stage can mutate the selected or existing variant, copy a
  stable untracked snapshot into separate external directories for final `v1`,
  final `v2`, pre-highlight `manual`, and post-highlight selected `manual`.
  Each snapshot must include the ARO YAML, HTML, PDF, ATS/lineage/model
  metadata, and every rendered PDF page. Exclude credentials, raw model
  traffic, and machine-specific paths from the preserved snapshot set.
- Expand the README with a clearly labeled recorded synthetic live traversal.
  Preserve `make demo` as the deterministic offline baseline and state that the
  recorded live outputs are non-deterministic examples, not release fixtures or
  factual authority.
- Retain the reviewed disposable live workspace through G04 as the source for
  P04's offline screenshots. Disable network/model execution before capture
  and never expose credentials or machine paths. Keep the four final variant
  snapshot directories untracked and mode-0700 until the user explicitly
  decides which, if any, should become public examples; do not delete them at a
  gate or closeout step without that later authority.

### G03R - Live Traversal and README Evidence Review

- Present the v1 → v2 → manual → highlighting lineage, content-free ATS
  movement, evidence-validation findings, selection transitions, artifact and
  PDF review, preserved-snapshot inventory and hashes, README diff, validation
  results, and all external calls made.
- If the renewed traversal stops early, present the completed prefix, exact
  sanitized retry/provider-boundary evidence, retained log and snapshot
  inventory, and the failed-stage category/subtype without fabricating the
  remaining stages or silently rerunning them.
- Require explicit user approval of the recorded traversal and its README
  treatment before P04. If any stage lacks grounded output or a safe retained
  workspace, pause rather than fabricating the missing example.
- Confirm strategy 2 for all nine Flask illustrations: regenerate them from
  the reviewed live synthetic workspace after it is placed into offline-only
  capture mode.

### P03S - First-Draft ATS Diagnostics Serialization Repair

- Preserve every completed P03R attempt as immutable history. Revision 10
  passed its model-free preflight and completed all nine v1 provider operations
  on attempt one, then failed before opening its state transaction because the
  first-draft producer passed tuple-bearing `asdict(AtsDiagnostics)` data into
  the strict JSON-only state contract. No live v1 was stored, no later stage
  ran, and that live authority is consumed.
- Repair the demonstrated functional defect in
  `scripts/application_resume_generate_drafts.py`: JSON-materialize the actual
  ATS diagnostics payload so nested tuples become plain lists before the
  `ResumeVariantWrite` reaches `ApplicationStateStore`. Preserve scalar values,
  mapping keys, scores, term ordering, and all content. Keep the state store's
  exact-list/dict validator strict; do not weaken it to accept arbitrary Python
  containers.
- Keep the implementation local to the demonstrated v1 producer path and its
  focused tests. Do not introduce a serialization framework, change retry or
  logging behavior, broaden state schemas, refactor adjacent workflows, or fix
  other raw-dataclass call sites without a separately demonstrated and approved
  scope. If the repair cannot remain this narrow, stop for review.
- Add a production-shaped regression that uses a real `AtsDiagnostics` object,
  the production first-draft call site, and a real `ApplicationStateStore` in a
  disposable synthetic workspace. The test must not replace `asdict`, replace
  diagnostics with a pre-normalized dictionary, or fake the state-store write.
  Prove the conditional v1 write commits, persists JSON-safe diagnostics,
  preserves expected-revision behavior and automatic selection, and retains
  the complete diagnostic values and order. A small direct assertion may also
  prove that the previously raw tuple-bearing payload is rejected while the
  materialized payload is accepted.
- Keep P03S entirely offline. Do not load the private dotenv, call any provider,
  model, or Codex subprocess, inspect or mutate the retained P03R workspace,
  read model traffic, or populate/delete the four snapshot directories. Do not
  consume or imply authority for another live traversal.
- Align version `2.1.0`, `CHANGELOG.md`, and
  `docs/release-notes/2.1.0.md` with the verified functional repair without
  claiming that a live v1 or later traversal now exists. Run focused generator
  and state-contract tests, proportional regression coverage, Ruff check and
  format check, `git diff --check`, release-metadata tests, and public-tree
  safety. Commit normally and stop at G03S with a clean worktree.

### G03S - Functional Serialization Repair Review

- Present the exact production cause, changed paths, tuple-to-list preservation
  proof, real-state-store v1 commit regression, expected-revision and selection
  proof, validation results, release-document alignment, and privacy/scope
  review.
- Require confirmation that this is an actual producer/state-contract fix—not
  another diagnostic layer—and that no state-validator relaxation, adjacent
  workflow refactor, retry change, deferred 2.2 diagnostics architecture,
  private-state access, or external call entered the repair.
- Pause for explicit user approval. P03S/G03S does not authorize a provider or
  Codex call. Any renewed synthetic live traversal remains a separate committed
  plan amendment and explicit approval boundary.

### P03T - Post-Serialization Recorded Synthetic Traversal

- Preserve every P03R attempt and P03S/G03S result as completed history. On
  2026-08-04 the user approved G03S and explicitly authorized one new
  post-fix synthetic traversal. This revision authorizes exactly one P03T
  sequence against commit `65691efdfc6e2eb4916aaa8679196f1f45139ee6` and
  no operator rerun. Reuse the preserved mode-0700 external Tessa Rowan and
  Rivermark workspace and its four empty snapshot directories; do not recreate,
  clear, delete, or substitute them.
- Before any provider or Codex subprocess call, run exactly one sanitized,
  model-free preflight through the intended Make execution path. It must prove
  that every workspace member and the resume template resolve canonically
  inside the synthetic workspace; the v1 and v2 API model is `z-ai/glm-5.2`;
  regular manual is `gpt-5.6-sol` with `high` effort; highlighting is
  `gpt-5.6-luna` with `high` effort; the timeouts are 300, 600, 900, and 900
  seconds; every retry count is two and every total-attempt count is three; and
  timeout/retry configuration comes from Make. Record only approved labels,
  counts, booleans, and hashes. Do not print credentials, raw paths, private
  dotenv values, prompts, outputs, URLs, or career content. Confirm the
  deterministic baseline database, selection, and artifact fingerprints are
  unchanged before live execution.
- A failed or ambiguous preflight ends this revision's execution authority and
  returns immediately to G03T with zero provider/Codex calls. Correcting and
  repeating the preflight or starting a live stage afterward requires another
  committed amendment and explicit user approval.
- After a passing preflight, invoke force-v1 exactly once and only for
  `demo-platform-001`, using `FIRST_DRAFT_LLM_RETRIES=2`,
  `SECOND_PASS_RETRIES=2`, and `CODEX_RETRIES=2`. Each value means at most two
  workflow-owned retries after the initial attempt, for no more than three
  provider boundaries per API-backed logical operation or three Codex
  subprocess attempts. Use the required process/network authority from the
  start of each sole live invocation; a sandbox-denied or wrapper-level retry
  is not permitted.
- Continue only after verified success of the immediately preceding stage:
  one v2 refinement, one `regular` manual pass derived from v2, and one
  highlighting pass against the automatically selected manual variant. Stop
  immediately at the first failed or ambiguous stage. Do not repair code or
  configuration, repeat a command, compensate with a wrapper retry, change the
  row, or skip ahead under this authority.
- After each successful stage, verify stored variant lineage, MRO/JOD evidence
  ownership, accepted/rejected/unsupported changes, ATS diagnostics, model
  metadata, selection and human-review state, and complete YAML/HTML/PDF
  artifacts. Render every PDF page with Poppler and visually inspect
  legibility, overlap, clipping, wrapping, margins, and factual completeness.
- Before any later stage can mutate the selected or existing variant, preserve
  a stable external snapshot in the already-created `final-v1`, `final-v2`, or
  `manual-pre-highlight` directory as appropriate. Immediately after successful
  highlighting and before any README update or G03T transition, populate the
  `manual-selected-highlighted` snapshot as well. Each required snapshot must
  include ARO YAML, HTML, PDF,
  ATS/lineage/model metadata, and every rendered PDF page. Keep the directories
  mode 0700, untracked, and retained for later user direction; exclude
  credentials, raw model traffic, and machine-specific paths.
- Preserve one full chronological sanitized run log in the external evidence
  area with mode 0600, using the existing P03C event/log path or bounded
  operator redirection. Record content-free configuration, attempt,
  provider-boundary, elapsed-time, retry-decision, completion, and closed
  category/subtype evidence. Do not add a logging framework or retain raw
  traffic, prompts, responses, credentials, URLs, exception text, or private
  content.
- Only if all four stages and artifact reviews pass, update the README with a
  clearly labeled recorded synthetic traversal grounded in the reviewed stored
  outputs. Preserve `make demo` as the deterministic offline baseline and label
  live outputs as non-deterministic examples rather than release fixtures. If
  the sequence stops early, leave the README unchanged and report only the
  completed prefix and sanitized failure evidence.
- This authority does not permit job search/fetch, browser or authenticated
  activity, application submission, employer contact, a different job row,
  retained private state, P04 asset work, another compatibility or diagnostics
  repair, push, PR, merge, tag, or release/publication action. Stop at G03T with
  the retained synthetic workspace placed into offline-only capture mode.

### G03T - Live Traversal and README Evidence Review

- Present the exact external-call inventory, sanitized configuration and
  attempt/boundary evidence, v1 → v2 → manual → highlighting lineage,
  content-free ATS movement, evidence-validation findings, selection
  transitions, artifact and per-page PDF review, preserved-snapshot inventory
  and hashes, README diff if one was authorized by full success, and validation
  results.
- If P03T stops early, present the completed prefix, failed-stage closed
  category/subtype, zero-retry versus retry-exhaustion status, retained log and
  snapshot inventory, and proof that no later stage or operator rerun occurred.
  Do not fabricate or approximate missing outputs.
- Require explicit user approval of the recorded traversal and its README
  treatment before P04. Confirm strategy 2 for all nine Flask illustrations
  only if a complete, reviewed synthetic workspace exists in offline-only
  capture mode; otherwise pause and amend future visual work.

### P03U - Hard Deadline and Embedded-Envelope Recovery

- Preserve P03C, P03D, P03R, P03S, and P03T as completed historical evidence.
  Revision 12 passed preflight; core, JOD, and three experience operations
  succeeded, then the fourth experience operation failed after 439.688 seconds
  as `category=model`, `failure_subtype=malformed_envelope`, and `retry=false`
  on attempt 1/3. All six provider boundaries used their first attempt, no v1
  was stored, no later stage ran, and the revision-12 live authority is
  consumed. The same operation succeeded in 16.333 seconds during revision 10,
  where all nine v1 calls completed before the separately repaired ATS state-
  write defect. Treat P03T prospectively as a stochastic provider-envelope
  outcome that exposed deterministic missing-hard-deadline and envelope-
  classification defects, not a prompt, context, model, or configuration
  regression; do not rewrite its retained evidence.
- Keep P03U offline. Do not resolve the private dotenv, call a provider/model
  or Codex subprocess, launch a live Flask workflow, inspect or mutate the
  retained synthetic workspace, populate/delete the four snapshot directories,
  update recorded synthetic examples, or consume authority for another live
  traversal.
- Enforce the already resolved v1/v2 timeout as a true wall-clock deadline
  around each logical model attempt, inside the workflow-owned attempt loop.
  Preserve the existing transport/inactivity timeout as defense in depth,
  keep backoff outside the per-attempt deadline, give each retry a fresh full
  deadline, await cancellation cleanup, and distinguish deadline-owned expiry
  from an arbitrary inner `TimeoutError` or external cancellation. Preserve
  provider-appropriate typed timeout reporting, v2 generation-JSON validation
  inside its logical operation, and unchanged manual/highlighting behavior.
- Keep one visible workflow-layer retry owner. Workflow-created API clients
  remain at one internal attempt, one outer attempt crosses the provider
  boundary at most once, and `retry_count` permits exactly
  `retry_count + 1` maximum attempts for one logical operation. Do not change
  configured retry defaults, create a whole-v1/v2 job timeout, add an operator
  retry, or broaden non-timeout Ollama failures.
- Inspect bounded HTTP-200 OpenRouter-compatible envelopes for validated top-
  level errors, first-choice errors, and `finish_reason=error` before accepting
  completion content. Add closed typed embedded-transient and embedded-
  permanent classifications without overloading the actual HTTP-status
  subtypes or parsing messages. Recognize rate limiting, provider/server
  unavailability or overload, upstream timeout, and the existing transient
  status equivalents. A permanent request/authentication/policy/context signal
  is non-retryable even when partial content is present. Unknown,
  contradictory, or structurally invalid error objects remain non-retryable
  malformed envelopes.
- When no permanent embedded error is present, classify a valid envelope with
  absent/null/empty choices or absent/null/empty usable message content as the
  existing retryable `empty_completion` condition. Never accept partial
  content whose choice ended with `finish_reason=error`. Keep wrong non-null
  structural types, malformed JSON or non-map envelopes, response-size
  violations, invalid generated JSON, valid-JSON schema/application failures,
  and evidence/policy/state/render/ATS/path/artifact failures non-retryable.
  Embedded-error classification must precede generated-content JSON
  validation.
- Extend typed errors and the existing exact diagnostic schema with only an
  optional immutable, content-free response summary on relevant retry/failure
  events. It may retain a bounded HTTP status/error code, closed normalized
  top-level/choice error presence and type, closed finish reason, bounded choice
  count, and closed content state. Never retain error messages, unknown raw
  provider strings, provider names, bodies, prompts, completions, URLs, paths,
  headers beyond already bounded safe `Retry-After` timing, credentials,
  exception text, or career content. Preserve stable broad categories, make
  new subtypes/metadata additive, and keep both legacy category-only records
  and existing subtype-only records readable.
- Use the existing P03C JSONL stderr event stream and Flask-owned bounded
  capture; the compact status UI may ignore the added detail. Do not add a
  universal direct/Make recorder, run IDs, leases, cross-process ownership,
  restart recovery, multi-job correlation, truncation aggregation, durable
  final-summary state, a structured viewer, or composite-Make ownership. Those
  remain one deferred 2.2.0 architecture. Prove the complete ordered sanitized
  event sequence can be captured at emission time into a mode-0600 test file.
  A separately authorized future traversal must use that source capture and
  record mode, schema validation, leakage scan, line count, and SHA-256; the
  Flask status projection is supplemental rather than unbounded evidence.
- Add focused offline tests for a delayed/chunked attempt that outlives the
  wall-clock limit and then succeeds; timeout exhaustion and cancellation;
  HTTP-200 transient top-level and choice-level errors followed by success;
  missing/null/empty completion followed by success; permanent, malformed,
  oversized, and invalid-generation responses without retry; exact provider-
  boundary counts; and equivalent v1/v2 behavior. Preserve the real
  `AtsDiagnostics` plus disposable `ApplicationStateStore` regression and
  focused guards for unchanged Ollama non-timeout and manual/highlighting
  contracts. Prove diagnostics contain only closed metadata and that retained
  legacy status records still load.
- Keep implementation centered on `workflow_retry.py`, `api_client.py`,
  `errors.py`, `workflow_diagnostics.py`, the v1 generator, and the v2 CLI.
  Touch `workflow_status.py` only if the additive schema requires safe
  propagation; do not redesign Flask or Make. Align README operational text,
  the retained `2.1.0` version, `CHANGELOG.md`, and
  `docs/release-notes/2.1.0.md` with verified behavior. Run focused tests, the
  complete suite, Ruff check/format, `git diff --check`, release-metadata and
  public-tree safety checks, plus proportional package/import smoke if public
  exports change. Commit normally with a clean worktree and stop at G03U.

### G03U - Hard-Deadline and Embedded-Error Contract Review

- Present the per-attempt wall-clock proof, deadline-owned timeout/cancellation
  proof, embedded top-level/choice/finish-reason classification matrix,
  retryable missing/null/empty completion proof, and exact
  `retry_count + 1` provider-boundary evidence for both v1 and v2. Confirm one
  workflow retry owner, one API internal attempt, bounded backoff, unchanged
  retry defaults, no whole-job deadline, and unchanged manual/highlighting and
  non-timeout Ollama behavior.
- Present additive typed-error and response-summary schemas, legacy status-
  record readability, complete ordered mode-0600 diagnostic-capture proof,
  Flask compatibility, ATS persistence regression, changed paths, version and
  release-document alignment, complete validation, commit, and clean-worktree
  evidence. Confirm no prompt, completion, response body, raw provider string,
  credential, URL, path, exception text, or career content was retained.
- Confirm that no deferred 2.2.0 logger, recorder ownership, lease, correlation,
  restart, aggregation, durable summary, viewer, or composite-Make architecture
  entered P03U and that no private workspace or external model/Codex activity
  occurred.
- Pause for explicit user review. G03U completion or approval authorizes no
  live call. Only after a G03U PASS and separate user approval may the
  supervisor commit and bind a distinct future P03V/G03V amendment for exactly
  one model-free-preflight-gated synthetic v1 → v2 → regular-manual →
  highlighting traversal, stopped at its first failure with retained snapshots,
  PDFs, and complete sanitized evidence.

### P03V - Post-P03U Recorded Synthetic Traversal

- Preserve P03R, P03S, P03T, P03U, and every matching gate as completed
  history. G03U passed after commits `ce22b04`, `6558f3c`, and `c02eead`
  established true per-attempt v1/v2 deadlines, typed HTTP-200 embedded-error
  recovery, exact retry ownership, additive sanitized response summaries, and
  legacy diagnostic compatibility. The user's revision-14 approval authorized
  only committing the distinct P03V/G03V amendment. Binding revision 14 did not
  begin P03V, resolve the private dotenv, access or mutate the retained
  synthetic workspace, dispatch an implementor, run a model-free preflight, or
  call a provider, model, or Codex subprocess.
- Revision 14's fresh-task preflight stopped during its initial tracker-binding
  check before creating evidence, resolving the private dotenv, accessing the
  retained workspace, or running a Make target. Its implementor used an
  incorrect standalone-scalar equality test for a task ID stored inside the
  P03V attempt note; the supervisor confirmed the binding was present and
  preserved the failed G03V result. Provider/model/Codex/network boundaries and
  workspace mutations were all zero.
- The user then explicitly directed that the single corrected preflight run in
  original implementor task `019fc9c1-9655-7bd1-8383-ead32f0923da`. Revision
  15 authorizes exactly that one task-reuse exception and one corrected,
  model-free preflight. The implementor owns the retained synthetic workspace
  and evidence for this attempt; the supervisor alone owns tracker transitions
  and G03V. The binding check must inspect the actual attempted-step note or
  perform a validated substring check, not repeat the failed scalar-equality
  test. This authority does not permit live execution.
- Revision 15's implementor-owned correction passed its binding, branch,
  commit, workspace, snapshot, internal-attempt, and static Make-composition
  checks, but its generated local `python -c` orchestration wrapper failed to
  compile because shell quoting altered a JSON-count literal. The failure
  occurred before evidence-file creation or any Make invocation. All four
  targets remained unrun; provider/model/Codex/network/attempt boundaries and
  retained-workspace mutations were all zero. Preserve that ambiguous G03V
  result and treat revision 15's authority as consumed.
- On 2026-08-04 the user explicitly approved one revision-16 correction owned
  directly by the supervisor task. Do not dispatch or delegate it and do not
  generate a Python or shell orchestration wrapper. After this amendment is
  committed and digest-bound, the supervisor may execute the four config-only
  Make targets as four separate direct commands against the same retained
  synthetic workspace, capturing their existing sanitized stderr events into
  one new unique mode-0600 evidence file. This is the sole revision-16
  preflight attempt and authorizes no live execution.
- Reuse the preserved external mode-0700 Tessa Rowan/Rivermark workspace for
  `demo-platform-001` and its four existing empty mode-0700 snapshot
  directories. Do not recreate, clear, delete, relocate, or substitute that
  workspace or those directories.
- Under revision 16's direct supervisor-owned preflight-only authority, run
  exactly one sanitized, model-free preflight through the intended Make/Flask
  execution path. Before
  touching provider or Codex boundaries it must prove the committed plan
  revision/digest, exact code commit, branch and clean worktree; canonical
  contained non-aliased workspace and template members; mode-0600 private
  dotenv selection and configured-credential booleans without values; the
  unchanged deterministic database, selection, artifact, and four-empty-
  snapshot fingerprints; job ID `demo-platform-001`; v1/v2 model
  `z-ai/glm-5.2`; regular manual model `gpt-5.6-sol` at `high`; highlighting
  model `gpt-5.6-luna` at `high`; timeouts 300/600/900/900 seconds; retry
  counts two and total attempts three; and Make as the timeout/retry source.
  Confirm one internal API attempt for workflow-created clients. Create one
  unique mode-0600 P03V JSONL file and capture the complete sanitized preflight
  stream at emission time without overwriting retained P03R/P03T evidence.
  Record only approved labels, booleans, counts, modes, and hashes. Never print
  credentials, dotenv values, raw paths, URLs, prompts, outputs, exception
  text, or career content.
- Stop at G03V after the preflight whether it passes, fails, or is ambiguous,
  with zero provider/model/Codex calls. Present its exit status, event count,
  SHA-256, exact-schema and leakage results, configuration sources,
  fingerprints, and provider-boundary count. A passing preflight requires a
  separate explicit user approval before G03V may continue back into P03V for
  the live subphase. Neither the amendment, P03V dispatch, nor preflight PASS
  supplies that approval. Revision 16 is the only approved correction after
  the revision-14 validator false negative and revision-15 wrapper failure;
  repeating or correcting it after any outcome requires another committed
  amendment and explicit user approval.
- Revision 16's direct preflight passed: all four config-only Make targets
  exited zero; the six exact-schema configuration events proved the approved
  models, efforts, timeouts, two retries/three total attempts, Make timeout and
  retry sources, configured workspace, and zero provider/model/Codex attempt
  events; the database, deterministic selection, artifacts, and four empty
  mode-0700 snapshots were unchanged; and the unique mode-0600 log passed its
  schema and leakage checks. On 2026-08-04 the user explicitly approved the
  live-subphase boundary presented at G03V. Revision 17 therefore authorizes
  exactly one supervisor-owned direct live sequence and no operator rerun.
- Under revision 17, invoke force-v1 exactly once and
  only for `demo-platform-001`, with `FIRST_DRAFT_LLM_RETRIES=2`,
  `SECOND_PASS_RETRIES=2`, and `CODEX_RETRIES=2`. Continue only after verified
  success of the immediately preceding stage: one v2 refinement, one
  `regular` manual pass derived from v2, then one highlighting pass against the
  automatically selected manual variant. Each setting permits at most two
  retries after the initial attempt. Keep one workflow retry owner, one
  internal API attempt, the P03U per-attempt wall-clock deadlines, and the
  existing manual/highlighting subprocess limits. Do not add a wrapper or
  operator retry.
- Before the approved live subphase, verify without rerunning preflight that
  the repository commit, plan digest, workspace and artifact fingerprints,
  directory modes, and retained preflight evidence have not drifted. Any drift
  pauses P03V without a provider/model/Codex call and requires renewed user
  direction.
- Stop immediately at the first failed or ambiguous stage. Do not repair code
  or configuration, repeat a command, change the target row or profile, skip
  ahead, or spend a new operator invocation merely to obtain complete demo
  output. Preserve the completed prefix and report whether a failure consumed
  zero retries, retried then recovered, or exhausted its configured budget,
  using only the closed diagnostic contract. Any rerun or repair requires a
  new committed amendment and explicit user approval.
- After each successful stage, verify stored variant lineage, MRO/JOD evidence
  ownership, accepted/rejected/unsupported changes, ATS diagnostics, model
  metadata, automatic/manual selection, human-review state, and complete ARO
  YAML/HTML/PDF artifacts. Before a later stage can mutate relevant state,
  populate the retained `final-v1`, `final-v2`, and
  `manual-pre-highlight` directories as applicable; immediately after
  successful highlighting populate `manual-selected-highlighted`. Each
  snapshot must include ARO YAML, HTML, PDF, ATS/lineage/model metadata, and
  every rendered PDF page. Render every page with Poppler and inspect
  legibility, overlap, clipping, wrapping, margins, and factual completeness.
  Keep all four directories mode 0700, untracked, and retained for later user
  direction; exclude credentials, raw model traffic, and machine-specific
  paths.
- Append the approved live stages to the unique external mode-0600 P03V JSONL
  file created during preflight, capturing the complete chronological stream
  at emission time rather than through the Flask last-20 projection. Validate
  its mode, exact schema, chronological line count, final-stage state, retry
  versus exhaustion evidence, SHA-256, and absence of prompts, completions,
  response bodies, raw provider strings,
  credentials, URLs, paths, raw exception text, or career content. The Flask
  full-detail status is supplemental. Do not add the deferred 2.2.0 recorder,
  run correlation, ownership/lease, restart, aggregation, durable-summary, or
  viewer architecture.
- Only if all four stages, artifact checks, PDF review, snapshot retention, and
  leakage checks pass may P03V update the README, changelog, and 2.1.0 release
  note with a clearly labeled recorded synthetic traversal grounded in the
  reviewed stored outputs. Preserve `make demo` as the deterministic baseline
  and label recorded live outputs as non-deterministic examples rather than
  fixtures or factual
  authority. If the sequence stops early, leave the README and public examples
  unchanged. In either case, disable network/model execution and place the
  retained workspace into offline-only capture mode before G03V.
- P03V does not authorize a job search/fetch, browser or authenticated
  activity, application submission, employer contact, private profile or
  application state, a different row, another code/configuration/diagnostic
  repair, P04 asset work, push, PR, merge, tag, or publication.

### G03V - Preflight and Post-P03U Traversal Evidence Review

- At the mandatory first checkpoint, present the exact sanitized preflight
  result, configuration sources, unchanged workspace/snapshot fingerprints,
  unique log mode/count/hash/schema/leakage evidence, and proof of zero
  provider/model/Codex calls. Confirm the revision-14 false-negative history,
  the revision-15 user-designated task and zero-call wrapper-failure history,
  revision-16 direct supervisor ownership and PASS, revision-17 live authority,
  and the clean ownership boundary. If P03V later stops
  during live execution, present the completed prefix, exact external-call and
  provider-boundary inventory, attempt/retry/exhaustion evidence, closed
  failure category and
  subtype, retained log and snapshot inventory, and proof that no later stage
  or operator rerun occurred.
- On full success, present the v1 → v2 → regular-manual → highlighted-
  manual lineage, content-free ATS movement, evidence-validation findings,
  selection transitions, stored artifact and per-page PDF review, all four
  snapshot hashes, sanitized log metadata, README diff, and validation results.
  Confirm one retry owner, exact maximum attempt budgets, per-attempt deadline
  behavior, and absence of private or raw provider content.
- Confirm that no deferred 2.2.0 diagnostics architecture, unapproved repair,
  job retrieval, authenticated/private state, extra invocation, README
  fabrication, P04 work, or release action entered P03V. Require explicit user
  approval of the traversal and any README treatment before P04.
- Confirm strategy 2 for all nine Flask illustrations only when all four live
  stages and their artifact reviews passed and the workspace is in offline-
  only capture mode. Otherwise pause and require an explicit future amendment
  selecting either another separately justified path or P04's deterministic-
  offline fallback; never treat a partial traversal as complete.

### P04 - Diagrams and Flask Visuals

- Recreate the master-resume-build and ARO-application SVG diagrams against the
  current public workflow, using only approved demo terminology and examples.
- Apply the latest separately approved successful recorded-traversal gate to
  each Flask view/tab image. Regenerate all nine illustrations from a complete,
  reviewed synthetic workspace only after that workspace exists in offline-
  only capture mode. If no complete reviewed live workspace exists, pause and
  amend P04 for explicit approval of a deterministic-offline fallback rather
  than treating any stopped traversal workspace as complete. Use offline
  Chromium and the local PID-owned website lifecycle; no provider, model, job
  fetch, or other external request may occur during capture.
- Preserve useful annotations, readable dimensions, consistent styling,
  descriptive alt text, and the archived README's visual teaching sequence.
- Validate SVG structure, raster readability, README rendering, image links,
  public-tree safety, and absence of private names, paths, browser chrome,
  tokens, or real application state.

### G04 - Visual and Documentation Reassessment

- Present the rendered README and changed visuals for explicit user review.
- Compare every diagram edge, label, screenshot annotation, and surrounding
  caption to current product behavior and the approved demo.
- Continue only if the visuals add parity without introducing unsupported
  claims or privacy ambiguity; otherwise revise P04 or amend future work.

### P05 - Integrated v2.1.0 Release Readiness

- Finalize the bootstrapped `pyproject.toml` version, top v2.1.0 changelog
  section, and v2.1.0 release note against the accepted documentation and demo
  changes.
- Run focused demo, README, packaging, and safety tests; Ruff check/format;
  `git diff --check`; and the public-tree safety command.
- Run release-readiness validation: the complete suite, wheel/sdist build and
  scan, isolated-wheel smoke, offline Chromium workbench, and injected MCP
  stdio checks. Use only synthetic disposable state.
- Verify tracked and built distributions contain the intended README, demo
  inputs, diagrams, screenshots, skills, and release metadata with no private
  material or machine-specific paths.
- Commit the completed local release candidate. Do not push or create a PR.

### G05 - Final Review and Release Boundary

- Present the final diff, parity summary, visual review set, validation matrix,
  commits, and remaining deviations from the bound archived README.
- Confirm the release remains documentation-centered and defers broader README
  improvement ideas to a later feature release.
- Pause for explicit authority before push, PR creation, merge, tag, package
  publication, or release publication.

## Pause Conditions

- The plan is not explicitly approved before P01.
- Private source text, MRO data, mappings, excerpts, identifiers, screenshots,
  or content-bearing evidence appears anywhere under the public repository.
- The candidate remains linkable to a real person or employer through direct
  identifiers, distinctive phrases, unique fact combinations, chronology, or
  organization-specific products and systems.
- The designated longest role cannot meet both the 45-55 percent reduction and
  the requested long-form substantive feel, or another role loses content
  beyond what de-identification requires.
- The public MRO contains a claim that cannot be traced to the approved public
  text, or the text/YAML pair diverges structurally.
- README parity would require claiming a feature, command, integration, model,
  artifact, or workflow the public v2.1.0 tree does not support.
- A screenshot contains private state, an unapproved real posting, a position
  whose application status is uncertain, credentials, or a machine path.
- A demo compatibility failure requires changing product behavior rather than
  making a narrow fixture, documentation, P03C, P03D, P03S, or completed P03U
  adjustment.
- P03T would repeat its preflight or any live stage, make more than the single
  authorized invocation per stage, continue after a failed or ambiguous stage,
  use a row or workspace other than the preserved synthetic target, alter code
  or configuration, expose content-bearing evidence, omit required snapshots
  or PDF review, or enter P04 before G03T approval.
- P03S would weaken the state-store JSON contract, change anything beyond the
  demonstrated first-draft ATS-diagnostics producer path and focused tests,
  add generalized serialization or diagnostics architecture, alter retries or
  workflow selection, touch retained live/private state, or authorize another
  provider/model/Codex invocation.
- P03D would broaden API retries beyond the typed transient allowlist, create
  more than `retry_count + 1` provider boundaries, alter non-timeout Ollama or
  manual/highlighting retry behavior, parse raw exception messages, make
  retained diagnostics unreadable, reintroduce the deferred durable-diagnostic
  architecture, or expose content, credentials, URLs, headers, paths, or
  provider-private text.
- P03U would allow a model attempt to outlive its configured wall-clock
  deadline, create nested or more than `retry_count + 1` attempts, relabel an
  unrelated `TimeoutError` or cancellation, parse or retain raw provider error
  text/body/content, retry a permanent or malformed envelope, accept partial
  `finish_reason=error` content, break legacy diagnostics, broaden unrelated
  provider/manual/highlighting behavior, implement deferred 2.2.0 diagnostics
  architecture, access private/live state, or call a provider/model/Codex
  subprocess.
- P03V would begin without revision-17 live authority, delegate or dispatch
  revision 16's supervisor-owned direct preflight, use a generated
  orchestration wrapper, repeat or correct the revision-16 preflight, resolve
  or expose private configuration outside the retained synthetic workspace,
  repeat any revision-17 live stage, use a
  different workspace/row/profile, alter code or configuration, omit the
  source-time mode-0600 log, snapshots, artifact checks, or PDF review,
  fabricate an incomplete README sequence, or enter P04 before G03V approval.
- The v2.0.0 publication baseline changes in a way that makes branch ancestry,
  version metadata, or changelog history ambiguous. Integrate reviewed upstream
  work normally; do not squash or rebase established release commits.
- Validation failure requires a broader refactor or unrelated quality/security
  remediation outside the documentation objective.
- The next action would call an external model, access authenticated/private
  state, mutate an external workspace, contact a third party, or perform push,
  PR, merge, tag, or publication work without the required authority.
